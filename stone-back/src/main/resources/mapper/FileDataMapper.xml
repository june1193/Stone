<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.mapper.FileDataMapper">
    <select id="selectAutoPayData" parameterType="string" resultType="map">
        SELECT *
          FROM FILE_TBL
         WHERE READ_YM = #{READYM}
    </select>
</mapper>
